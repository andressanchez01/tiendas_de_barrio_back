<h1>Perfil de Usuario</h1>
<table>
  <tr>
    <th>Atributo</th>
    <th>Valor</th>
  </tr>
  <% @user.attributes.each do |attr, value| %>
    <tr>
      <td><%= attr.humanize %></td>
      <td><%= value %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Editar Perfil', edit_user_path(@user) %>